# Kami Desktop

The desktop app is a Tauri v2 application with a React frontend and Rust backend.

Run it from the repo root with:

```bash
vp dev
```

Builds and releases are driven from the root repo. See `../../README.md` and `../../docs/releasing.md`.
